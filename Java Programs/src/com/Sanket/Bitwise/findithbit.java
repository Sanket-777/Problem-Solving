package com.Sanket.Bitwise;

public class findithbit {
    public static void main(String []args)
    {
       // n &(1 << n-1) &ing with the mask :- N & (1 <<( N-1))  leftshift(<<)
        // I have to create a mask with N-1 zeros becase it will be if 5th element then 10000 this
        //so obtain this do leftshit 1 n-1 time
    }
}
